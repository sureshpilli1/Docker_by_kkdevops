
--> Intro of docker
--> Docker arch
--> docker workflow
--> Before docker ,  After docker
--> docker installations
--> docker images related commands 
--> docker container related commands
--> Dockerfile 
--> Docker best practices

--> docker networks
--> Docker ECR
--> docker volumes
--> docker compose
----> k8s















Docker Networks
===============

--> Docker networks are used to establish communication between containers,services, and the outside world.
-->  proper networking ensures that your services are discoverable,secure, and scalable.
--> If both the containers are same network we can establish the connection.
--> If you are not mention any network, while creating a container, It will be created in "default bridge network".
-->Every Docker container gets its own private IP address within the network it belongs to. This
   IP is:
   . Unique within the Docker network
   . Assigned dynamically by Docker's internal DNS system
   . Can be seen using docker inspect <container>
   . This IP allows containers to talk directly to each other-even without exposing ports to the host.
--> By default docker have three networks
     1. bridge [default]
     2. host
     3. none
1. Bridge (Default)
When you create a container without specifying a network, it goes into the default bridge network.
Containers can talk to each other using IP, but not by name unless it's a custom bridge.
docker inspect <cid>
The container is connected to the default bridge network
It was assigned a private IP address.

Custom Bridge Network in Docker:
docker network create -d bridge jio-network
Create a container named three and attach it to the custom bridge network jio-network
three can communicate using ***container names*** with other containers in the same jio-network
Docker provides internal DNS resolution in custom bridge networks
We want to connect container one (which is in default network) to the custom network jio-network,

2.Host Network
The Host Network mode allows a container to use the host's networking stack directly
The container has no separate IP address.
It shares the host's IP and all its network interfaces.
Use Cases: This mode is ideal for applications that require high performance and low latency,
as it eliminates extra overhead from Docker's network layer.
Limitations: You cannot create custom networks in Host mode, so all containers share the
same network without isolation.

3.None Network
The container is fully isolated - no internet, no internal networking.
No interface is assigned.
Use this when you don't want the container to communicate with anything - not even localhost.
You'll see almost no network interfaces inside the container.
When to Use:
Security-critical containers (e.g. scanning, secure data processing)
For manual custom networking setup

What is Docker ECR (Amazon Elastic Container Registry)?
Amazon ECR (Elastic Container Registry) is a fully managed Docker container registry provided
by AWS.
Think of it as a private version of DockerHub, hosted and secured by AWS.
You can:
. Store Docker images securely
. Push images from any Docker server
. Pull images from anywhere (EC2, ECS, local, etc.)
How to Push Docker Image to AWS ECR
Step 1: Install unzip
Step 2: Install AWS CLI in Linux
Step 3: Get AWS Access Key & Secret Key
Step 4: Configure AWS Credentials in Server
Step 5: Create an ECR Repository
 1. Go to AWS Console
 2. Search and open ECR (Elastic Container Registry)
 3. Click Create Repository
 4. Give the ECR repository the name you want to create--> example KKFUNDA
 5. Cick Create, You will now see kkfunda repository created.
Step 6: View Push Commands from the ECR Console
After creating the repository:
1. Click on your repo kkfunda
2. You will see a section called View push commands
This section gives you all steps you need:
· For login
. Tagging the image
. Pushing the image to the repo
Step 7: Authenticate Docker with ECR (Temporary Token)
You'll see a login command to authenticate your Docker client to ECR from AWS. Run it like this:
. If login is successful, you'll see: Login Succeeded
. This token is temporary and valid for 12 hours, so repeat this step when needed.
Step 8: Tag Your Existing Docker Image
Suppose you already have this image:
kkeducation12345/javawebapp:1.0.0
. You cannot push it directly to ECR because the image must follow the ECR repository path
format. So, we need to tag it properly.
. In the official push commands (as shown in your image), AWS gives an example like:
 After the build is completed, tag your image so you can push the image to this repository:
Step 9: Push Image to ECR
Now push the newly tagged image:
The image will now be uploaded and visible in your ECR repository.
Step 10: Pull the Image from ECR (Test It)
Let's test by deleting the local image and pulling it from ECR:


How to list a docker networks?
docker network ls

How to see the container is created in which network?
docker inspect <cid>

How to check both the containers are connected or not?
step 0: create a two containers.
step 1: go inside the container one
docker exec -it mavenwebapp /bin/bash
step 2: ping with another container ip
yum update
yum install -y iputils
ping 172.17.0.3
curl -v telnet://172.17.0.3:8080
NOTE: In default bridge network only ip's will work to connect another container not a container names.
Dis advantage of bridge n/w: If container restarted we are not guarantee to get same IP

IQ] what is the difference between default bridge network and custom bridge network?
Ans: Incase of default bridge network the communication can happen through only IP address. Where as in custom bridge network the communication can happen through the IP address/ container name.

How to create a network in docker?
Syntax: docker network create -d <driver> <networkname>
EX: docker network create -d bridge jionetwork
docker network ls

How IPs are allocating to container?
docker inspect <network-name>

How to remove a unused networks?
docker system prune or docker network prune  or docker network rm <nwtwork-name>
Unused networks = Not attached to any container.
Optional: Remove a Specific Network
docker network rm airtelnw
bridge, host, and none (default Docker networks) cannot be removed.


How to create a container in network?
docker run -d -p 8080:8080 --network jionetwork --name mavenwebapp kkeducation123456/maven-web-app:1.2
docker run -d -p 8081:8080 --network jionetwork --name javawebapp kkeducation123456/maven-web-app:1.2
docker exec -it javawebapp bash
ping mavenwebapp

NOTE: same container might part of another networks also.
How to add that container to another network?
docker network connect  jionetwork javawebappbridge
docker inspect javawebappbridge

How to disconnect the container from network?
docker network disconnect  jionetwork javawebappbridge
docker inspect javawebappbridge

What is docker host network?
If we create container in host network, Container will not have an IP address.
NOTE: We can not create more than one container with same container port in the host network.
NOTE: you can not create custom host network like bridge
EX: docker run -d --network host --name javawebapp kkeducation123456/maven-web-app:1.2
     docker inspect 1ef68b5d5d80

What is none network?
--> If you create containers in none network, Container will not have an IP address.
--> we can not access these containers from outside or from any containers or internally.

NOTE: The scope of the bridge/host/none with the docker server only, To overcome this problem we are using "overlay" network [multi host networks]

IQ] what is overlay network?
Establishing connection between containers in different docker servers.



================================================================






BLOG NOTES:
-----------
0.1 Introduction
1 Why is Docker Networking Important?
2 Containers Have Their Own IP Addresses
3 1. Bridge (Default)
3.1 Step1. Check Available Images and Run a Container (Default Settings)
3.2 Step2. Inspect the Container
3.3 Step 3.Now let's launch a second container named two
3.4 Testing Container Communication (Default Bridge Network)
3.5 Step 1: Enter container one and use curl to connect to two
3.6 Step 2: Enter container two and curl back to one
3.7 Testing Communication Using ping (Default Bridge Network)
3.8 Step 3 : Enter container one and use ping to reach two
3.9 use ping to reach two from one
3.10 Step 4: Enter container two and ping back to one
4 2. Custom Bridge Network in Docker
4.1 Step 1: Create a Custom Bridge Network
4.2 Step 2: Launch Containers on the Custom Network
4.3 Step3.Inspect the Container
4.4 Step 4.Create Container four in jio-network
4.5 Step 5: Test Name-Based Communication
4.6 Step 6: Connect a Container to an Additional Network
4.7 Step 1: Connect one to jio-network
4.8 Step 2: Verify using inspect
4.9 Step 7: Docker Network: Create & Clean Up
4.10 Optional: Remove a Specific Network
4.11 Note:
5 3.Host Network
6 4. None Network
6.0.1 When to Use:
7 What is Docker ECR (Amazon Elastic Container Registry)?
8 How to Push Docker Image to AWS ECR
9 Step 1: Install unzip
10 Step 2: Install AWS CLI in Linux
11 Step 3: Get AWS Access Key & Secret Key
12 Step 4: Configure AWS Credentials in Server
13 Step 5: Create an ECR Repository
14 Step 6: View Push Commands from the ECR Console
15 Step 7: Authenticate Docker with ECR (Temporary Token)
16 Step 8: Tag Your Existing Docker Image
17 Step 9: Push Image to ECR
18 Step 10: Pull the Image from ECR (Test It)
19 Summary


Introduction
In the world of containerization, Docker networking plays a vital role in enabling
communication between containers, services, and the outside world. Whether you're building
simple applications or managing complex microservices architectures, understanding how
Docker containers interact over different network types is key to building secure, scalable,
and reliable environments.
This guide walks you through:
. The importance of Docker networking
. Key Docker network types like bridge, host, none, and custom networks
. Testing with containers using default and custom bridge networks
. Using container names for communication with Docker's internal DNS
. Steps to connect containers to multiple networks
. And finally, pushing Docker images to AWS Elastic Container Registry (ECR)-a secure and
scalable way to manage your container images in the cloud.
Why is Docker Networking Important?
Docker networking plays a crucial role in containerized environments. It ensures that:
. Containers can talk to each other (communication).
. Containers can access the internet.
. You can control the visibility and isolate services.
When working on microservices, proper networking ensures that your services are discoverable,
secure, and scalable.
Containers Have Their Own IP Addresses
Yes! Every Docker container gets its own private IP address within the network it belongs to. This
IP is:
. Unique within the Docker network
. Assigned dynamically by Docker's internal DNS system
. Can be seen using docker inspect <container>
This IP allows containers to talk directly to each other-even without exposing ports to the host.
In Docker, we have three networks:
· bridge
. host
none
1. Bridge (Default)
. This is the default network driver.
. When you create a container without specifying a network, it goes into the default
bridge network.
. Containers can talk to each other using IP, but not by name unless it's a custom bridge.
Example:
Step1. Check Available Images and Run a Container (Default Settings)
Let's start by checking the local images we have:
Now run a container without specifying any network:
Since we didn't specify a network, Docker automatically connects it to the bridge network.

Step2. Inspect the Container
To check which network the container is in and its IP address, run:
This confirms:
. The container is connected to the default bridge network
. It was assigned a private IP address (172.17.0.2)

Step 3.Now let's launch a second container named two
This launches two using the same bridge network by default.
List the container and inspect it.
. Look for the Networks section: Scroll down and look for the Networks section. You'll see that
both containers are connected to the bridge network.
Since we didn't specify any network while launching the containers, Docker automatically
connected them to the default bridge network. So, both one and two are part of the same
network by default.

Testing Container Communication (Default Bridge Network)
You can ping or curl using IP addresses only.
We already have:
. Container one: IP -> 172.17.0.2
. Container two: IP -> 172.17.0.3
curl -v telnet://<ip> :< port> checks TCP connection.
Step 1: Enter container one and use curl to connect to two
docker exec -it one bash
curl -v telnet: //172.17.0.3:8080
If two is running a service (like a web server), this should connect.

Step 2: Enter container two and curl back to one
Again, you'll get a response only if a service is running on the other side - but this shows they
can reach each other by IP.
This proves that containers on the default bridge network can communicate via IP, even without
a custom network.
Testing Communication Using ping (Default Bridge Network)

Step 3 : Enter container one and use ping to reach two
If the ping command is not found, update and install it inside the container.
# Inside container:
yum update -y
yum install -y iputils
To install ping (part of iputils) in YUM-based containers:
use ping to reach two from one
. ping sends ICMP Echo Request packets to the specified IP address.
. If the target container is running and connected to the same network, it replies with ICMP
Echo Reply packets.
. This confirms network-layer connectivity (i.e., the containers can see each other over the
network).
Note:
. ping uses ICMP (Internet Control Message Protocol), not TCP or UDP.
. It operates at the network layer (Layer 3) of the OSI model.
. It does not check if a specific port or service is running, only if the host is reachable.

Step 4: Enter container two and ping back to one
Using ping verifies that containers on Docker's default bridge network can reach each other at the
network level using ICMP.
. By default, Docker creates a bridge network named bridge.
. Containers attached to this network can communicate using IP addresses.
. However, container names are not resolvable by default on this network.
This means:
You cannot use container names to reach other containers on the default bridge network.
2. Custom Bridge Network in Docker
By creating a user-defined bridge network, Docker allows containers to communicate using container names, thanks to internal DNS support.

Step 1: Create a Custom Bridge Network
By default, Docker uses the bridge driver when you create a network without specifying a
driver.
docker network create -d bridge jio-network
or
docker network create jionetwork
Step 2: Launch Containers on the Custom Network
Create a container named three and attach it to the custom bridge network jio-network
. three can communicate using container names with other containers in the same jio-
network
. Docker provides internal DNS resolution in custom bridge networks
docker run -d -p 9091:9091 -- name three -- network jio-network
kkeducation12345/javawebapp:1.0.0
Step3.Inspect the Container
ubuntu@ip-172-31-12-86 :~ $
ubuntu@ip-172-31-12-86 :~ $ docker inspect three
The container is attached to the jio-network
It has its own private IP within the jio-network.
It's ready for name-based communication
Step 4.Create Container four in jio-network
ubuntu@ip-172-31-12-86 :~ $ docker run -d -p 9092:9092
-- name four -- network jio-network kked
7e19cc5fc21e84e939bf62327700b650066303a81305fda2f99808809341cc30
ubuntu@in-172-31-12-86 :~ $
This container, named four, is created in the jio-network. Now we have both three and four
containers in the jio-network.
ucation12345/javawebapp:1.0.0
Step 5: Test Name-Based Communication
Enter Container three
Ping Container four by Name and IP
. This sends ICMP (ping) packets to container four using both its name and IP address,
confirming successful communication in the custom bridge network.
. In a custom bridge network, Docker enables name-based communication between
containers using its built-in DNS system. This is not available in the default bridge network

Step 6: Connect a Container to an Additional Network
Now we have 4 containers:
on default bridge network
. three and four
. one and two
on custom bridge network (jio-network)
We want to connect container one (which is in default network) to the custom network jio-
network,
so that one can communicate with three and four
Step 1: Connect one to jio-network
Step 2: Verify using inspect
docker inspect one
Look under "Networks" section. You will now see:
This confirms that container one is connected to two networks:
. Default bridge
. Custom jio-network
Now Container one can:
. Talk to two via default bridge
. Talk to three and four via jio-network
Step 7: Docker Network: Create & Clean Up
. To create a custom bridge network named airtelnw
. This creates a user-defined bridge network called airtelnw.
. Check the Created Network
docker network ls
Remove Unused Networks
To remove all unused Docker networks (not used by any container):
docker network prune
You will be prompted to confirm:
type y and hit enter
· airtelnw is deleted (because no containers use it)
. jio-network stays (because it's mapped to mycontainer)
Optional: Remove a Specific Network
If you want to delete only the airtelnw network and no containers are using it:
docker network rm airtelnw
Note:
. Unused networks = Not attached to any container.
. bridge, host, and none (default Docker networks) cannot be removed.

3.Host Network
The Host Network mode allows a container to use the host's networking stack directly
. The container has no separate IP address.
. It shares the host's IP and all its network interfaces.
. Use Cases: This mode is ideal for applications that require high performance and low latency,
as it eliminates extra overhead from Docker's network layer.
. Limitations: You cannot create custom networks in Host mode, so all containers share the
same network without isolation.

4. None Network
The container is fully isolated - no internet, no internal networking.
. No interface is assigned.
. Use this when you don't want the container to communicate with anything - not
even localhost.
. You'll see almost no network interfaces inside the container.
When to Use:
. Security-critical containers (e.g. scanning, secure data processing)
. For manual custom networking setup

What is Docker ECR (Amazon Elastic Container Registry)?
Amazon ECR (Elastic Container Registry) is a fully managed Docker container registry provided
by AWS.
Think of it as a private version of DockerHub, hosted and secured by AWS.
You can:
. Store Docker images securely
. Push images from any Docker server
. Pull images from anywhere (EC2, ECS, local, etc.)
How to Push Docker Image to AWS ECR
Step 1: Install unzip
For Ubuntu/Debian systems:
sudo apt update
sudo apt install unzip -y
Step 2: Install AWS CLI in Linux
First, install AWS CLI on your Docker server
To install the latest AWS CLI v2 on a Linux server (like Ubuntu or CentOS).
The official AWS documentation provides instructions for installing or updating the AWS
Command Line Interface (CLI):
Installing or updating to the latest version of the AWS CLI
curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
unzip awscliv2.zip
sudo ./aws/install
To check it's installed:
aws -- version
Step 3: Get AWS Access Key & Secret Key
Go to your AWS account:
1. Click your username (top-right)
2. Choose Security Credentials
3.Go to the access keys 
4.Click create access key
5.Download the keys for future use
These are needed to connect your server with AWS.
Step 4: Configure AWS Credentials in Server
. When you want to push or pull Docker images using ECR, you need to use aws configure.
Without it, your AWS CLI and Docker won't have permission to communicate with ECR.
. We use aws configure to authenticate and connect our Docker server with AWS ECR by
using access keys.
. Run the aws configure command.
aws configure
Enter the AWS Access Key ID, AWS Secret Access Key, Region, and Output format.
Step 5: Create an ECR Repository
1. Go to AWS Console
2. Search and open ECR (Elastic Container Registry)
3.Click Create Repository
4.Give the ECR repository the name you want to create--> example KKFUNDA
5. Cick Create, You will now see kkfunda repository created.
Step 6: View Push Commands from the ECR Console
After creating the repository:
1. Click on your repo kkfunda
2. You will see a section called View push commands
This section gives you all steps you need:
· For login
. Tagging the image
. Pushing the image to the repo
Step 7: Authenticate Docker with ECR (Temporary Token)
You'll see a login command to authenticate your Docker client to ECR from AWS. Run it like this:
. If login is successful, you'll see: Login Succeeded
. This token is temporary and valid for 12 hours, so repeat this step when needed.
Step 8: Tag Your Existing Docker Image
Suppose you already have this image:
kkeducation12345/javawebapp:1.0.0
. You cannot push it directly to ECR because the image must follow the ECR repository path
format. So, we need to tag it properly.
. In the official push commands (as shown in your image), AWS gives an example like:

3. After the build is completed, tag your image so you can push the image to this repository:
docker tag kkfunda:latest 381491819887.dkr.ecr.eu-north-1.amazonaws. com/kkfunda: latest
. But in your case, you already have a built image: kkeducation12345/javawebapp:1.0.0
. So you don't need to build again. You only need to tag your existing image with the
proper ECR repo format.
Correct Tag Command for Your Image you should do this:
docker tag kkeducation12345/javawebapp:1.0.0 381491819887.dkr.ecr.eu-north-
1. amazonaws . com/kkfunda: latest
Tag the image and check it.
This is just like how we tag for Docker Hub (e.g., docker tag image username/repo:tag), but
here we use the ECR repository URI.
Step 9: Push Image to ECR
Now push the newly tagged image:
The image will now be uploaded and visible in your ECR repository.
Step 10: Pull the Image from ECR (Test It)
Let's test by deleting the local image and pulling it from ECR:
. This removes the image locally. Useful if you want to verify pulling it from ECR again.
. This pulls the image from your private ECR repository. Verify the pulled image.
This will list the pulled image
Summary
. ECR = Private Docker image store on AWS.
. We need to create a repo, login, tag, and push the image.
. This is better than Docker Hub when security and privacy matter.
. Always tag using the ECR path format.






















 



